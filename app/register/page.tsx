'use client';
import Link from 'next/link';import{useState}from'react';import{useRouter}from'next/navigation';import{traineeRepository}from'@/lib/data/repositories/trainee-repository';
export default function Register(){const[name,setName]=useState('');const[email,setEmail]=useState('');const[phone,setPhone]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[running,setRunning]=useState(false);const r=useRouter();
 async function submit(e:React.FormEvent){e.preventDefault();setMsg('');setRunning(true);try{if(await traineeRepository.findByEmail(email)){setMsg('هذا البريد مستخدم بالفعل.');return}const parts=name.trim().split(/\s+/);const u=await traineeRepository.create({profile:{firstName:parts[0]||'',lastName:parts.slice(1).join(' ')},contact:{email,phone},email,passwordHash:password,enrollments:[],progress:[],certificates:[],status:'active',emailVerified:true});await traineeRepository.loginUser(email,password);r.push('/account')}catch{setMsg('تعذر إنشاء الحساب. حاول مرة أخرى.')}finally{setRunning(false)}}
 return <main dir="rtl" className="auth-page"><form className="auth-card" onSubmit={submit}><h1>إنشاء حساب</h1><p>أنشئ حسابك للتسجيل في الدورات ومتابعة طلباتك.</p><label>الاسم الكامل<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>البريد الإلكتروني<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>رقم الجوال<input required value={phone} onChange={e=>setPhone(e.target.value)}/></label><label>كلمة المرور<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)}/></label>{msg&&<div className="auth-error">{msg}</div>}<button disabled={running}>{running?'جاري إنشاء الحساب...':'إنشاء الحساب'}</button><p className="auth-register">لديك حساب؟ <Link href="/login">تسجيل الدخول</Link></p></form></main>}
