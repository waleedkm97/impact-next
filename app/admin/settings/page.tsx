'use client';
import{useEffect,useState}from'react';import{settingsRepository}from'@/lib/data/repositories/settings-repository';
export default function Settings(){const[s,setS]=useState<any>(null);const[siteName,setSiteName]=useState('');const[email,setEmail]=useState('');const[phone,setPhone]=useState('');const[saved,setSaved]=useState(false);useEffect(()=>{void settingsRepository.findAll().then(x=>{setS(x);setSiteName(x.general.siteName ?? '');
setEmail(x.contact.email ?? '');
setPhone(x.contact.phone ?? '');})},[]);async function save(e:React.FormEvent){e.preventDefault();const x=await settingsRepository.update({general:{siteName},contact:{email,phone}} as any);setS(x);setSaved(true);setTimeout(()=>setSaved(false),1800)}if(!s)return <main className="admin-page">جاري التحميل...</main>;return <main className="admin-page"><header className="admin-page-header"><div><div className="eyebrow">Admin</div><h1>الإعدادات</h1><p>الإعدادات العامة وبيانات التواصل للموقع.</p></div></header><form className="admin-card" style={{padding:22,maxWidth:850}} onSubmit={save}><div className="admin-form-grid"><div className="admin-field"><label>اسم الموقع</label><input className="admin-input" value={siteName} onChange={e=>setSiteName(e.target.value)}/></div><div className="admin-field"><label>البريد الإلكتروني</label><input className="admin-input" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="admin-field"><label>الهاتف</label><input className="admin-input" value={phone} onChange={e=>setPhone(e.target.value)}/></div><div className="admin-field"><label>العملة</label><input className="admin-input" value={s.general.currency} readOnly/></div></div><div className="admin-modal-footer" style={{padding:'20px 0 0',border:0}}><button className="admin-btn admin-btn-primary">حفظ التغييرات</button>{saved&&<span style={{color:'#198754',fontSize:13}}>تم الحفظ ✓</span>}</div></form></main>}
