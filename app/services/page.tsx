'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
import { serviceRepository } from '@/lib/data/repositories/service-repository';
import type { Service } from '@/types/service';
import { useLocale } from '@/hooks/use-locale';
export default function ServicesPage() { const [services, setServices] = useState<Service[]>([]); const { isEnglish } = useLocale(); useEffect(() => { serviceRepository.findAll().then(setServices); }, []); return <main><section className="section services-section"><div className="section-inner"><div className="section-header"><p className="eyebrow">{isEnglish ? 'Our services' : 'خدماتنا'}</p><h2>{isEnglish ? 'Integrated learning and consulting solutions' : 'حلول تدريبية واستشارية متكاملة'}</h2><p className="section-lead">{isEnglish ? 'Specialized solutions that help organizations develop capability, improve performance and create measurable impact.' : 'نقدم حلولاً متخصصة تساعد منشأتك على تطوير الكفاءات وتحسين الأداء وتحقيق أثر ملموس.'}</p></div><div className="service-grid"><article className="service-card"><h3>{isEnglish ? 'Training disclosure' : 'الإفصاح التدريبي'}</h3><p>{isEnglish ? 'Support for training disclosure, documentation and follow-up through Qiwa.' : 'نساعد منشأتك على استكمال الإفصاح التدريبي عبر منصة قوى من خلال التدريب والتوثيق والمتابعة.'}</p><Link href="/training-disclosure" className="btn-secondary">{isEnglish ? 'View details' : 'عرض التفاصيل'}</Link></article>{services.map(s => <article key={s.id} className="service-card"><h3>{isEnglish ? 'Professional learning service' : s.title}</h3><p>{isEnglish ? 'A focused service designed to support capability development and measurable workplace impact.' : (s.shortDescription || s.description)}</p><Link href={`/service-details?id=${s.id}`} className="btn-secondary">{isEnglish ? 'View details' : 'عرض التفاصيل'}</Link></article>)}</div></div></section></main>; }

