import type { Order, OrderQuery, OrderFilter, OrderStatus, PaymentStatus } from '@/types/order';
import { DEFAULT_ORDERS } from '@/lib/data/seed-data';
import { browserDbGet, browserDbSet, migrateLegacyData } from '@/lib/data/browser-db';
let orders:Order[]=(DEFAULT_ORDERS as any[]).map((o:any,i:number)=>({id:o.id,orderNumber:o.id,customer:{traineeId:'',name:o.customer,email:o.email},items:[{id:`item-${i}`,type:'course',itemId:o.courseId,title:o.product,quantity:1,unitPrice:Number(String(o.amount).replace(/[^0-9.]/g,''))||0,totalPrice:Number(String(o.amount).replace(/[^0-9.]/g,''))||0}],subtotal:Number(String(o.amount).replace(/[^0-9.]/g,''))||0,discount:0,tax:0,total:Number(String(o.amount).replace(/[^0-9.]/g,''))||0,currency:'SAR',payment:{method:'credit-card',status:'paid',amount:Number(String(o.amount).replace(/[^0-9.]/g,''))||0,currency:'SAR'},status:o.status==='مكتمل'?'completed':'processing',createdAt:new Date(o.date),updatedAt:new Date(o.date)}));
let hydrated=false; let hydration:Promise<void>|null=null;
async function ensureHydrated(){if(hydrated)return;await migrateLegacyData();if(!hydration){hydration=(async()=>{const saved=await browserDbGet<Order[]>('orders');if(saved !== null)orders=saved.map(o=>({...o,createdAt:new Date(o.createdAt),updatedAt:new Date(o.updatedAt),confirmedAt:o.confirmedAt?new Date(o.confirmedAt):undefined,completedAt:o.completedAt?new Date(o.completedAt):undefined,cancelledAt:o.cancelledAt?new Date(o.cancelledAt):undefined,bookingDate:o.bookingDate?new Date(o.bookingDate):undefined,payment:{...o.payment,paidAt:o.payment.paidAt?new Date(o.payment.paidAt):undefined}}));hydrated=true;})().catch(()=>{hydrated=true;});}await hydration;}
async function persist(){await browserDbSet('orders',orders);}
export class OrderRepository{async findById(id:string){await ensureHydrated();return orders.find(o=>o.id===id)??null}async findByOrderNumber(n:string){await ensureHydrated();return orders.find(o=>o.orderNumber===n)??null}async findAll(q?:OrderQuery){await ensureHydrated();let r=orders.filter(o=>{const f=q?.filter;if(!f)return true;if(f.status&&o.status!==f.status)return false;if(f.paymentStatus&&o.payment.status!==f.paymentStatus)return false;if(f.traineeId&&o.customer.traineeId!==f.traineeId)return false;if(f.searchQuery&&!`${o.orderNumber} ${o.customer.name} ${o.customer.email}`.toLowerCase().includes(f.searchQuery.toLowerCase()))return false;return true});const o=q?.offset??0;return typeof q?.limit==='number'?r.slice(o,o+q.limit):r.slice(o)}async create(input:any){await ensureHydrated();const n=new Date();const o={...input,id:input.id??`order-${Date.now()}`,orderNumber:input.orderNumber??input.id??`IMP-${Date.now()}`,createdAt:n,updatedAt:n};orders.push(o);await persist();return o}async update(id:string,u:any){await ensureHydrated();const i=orders.findIndex(o=>o.id===id);if(i<0)throw new Error('Order not found');orders[i]={...orders[i],...u,updatedAt:new Date()};await persist();return orders[i]}async delete(id:string){await ensureHydrated();orders=orders.filter(o=>o.id!==id);await persist()}async updateStatus(id:string,status:OrderStatus){await ensureHydrated();return this.update(id,{status})}async updatePaymentStatus(id:string,status:PaymentStatus){await ensureHydrated();const o=await this.findById(id);if(!o)throw new Error('Order not found');return this.update(id,{payment:{...o.payment,status}})}async confirmOrder(id:string){await ensureHydrated();const o=await this.findById(id);if(!o)throw new Error('Order not found');const updated=await this.update(id,{status:'confirmed',confirmedAt:new Date(),payment:{...o.payment,status:'paid',paidAt:new Date()}});const item=o.items?.[0];if(o.customer?.traineeId&&item?.itemId){const { traineeRepository }=await import('./trainee-repository');const t=await traineeRepository.findById(o.customer.traineeId);if(t&&!t.enrollments.some(e=>e.courseId===item.itemId)){await traineeRepository.enrollInCourse(t.id,item.itemId,item.title);}}return updated}async cancelOrder(id:string,reason?:string){await ensureHydrated();return this.update(id,{status:'cancelled',notes:reason})}async findByTraineeId(id:string,q?:OrderQuery){await ensureHydrated();return this.findAll({...q,filter:{...q?.filter,traineeId:id}})}async findActiveOrders(id?:string){await ensureHydrated();return this.findAll({filter:{traineeId:id}}).then(r=>r.filter(o=>['pending','confirmed','processing'].includes(o.status)))}async findCompletedOrders(id?:string){await ensureHydrated();return this.findAll({filter:{traineeId:id}}).then(r=>r.filter(o=>o.status==='completed'))}async findByCouponId(id:string){await ensureHydrated();return orders.filter(o=>o.coupon?.couponId===id)}async getCount(f?:OrderFilter){await ensureHydrated();return (await this.findAll({filter:f})).length}async getTotalRevenue(f?:OrderFilter){await ensureHydrated();return (await this.findAll({filter:f})).filter(o=>o.payment.status==='paid').reduce((a,o)=>a+o.total,0)}async getRecentOrders(limit=10){await ensureHydrated();return [...orders].sort((a,b)=>b.createdAt.getTime()-a.createdAt.getTime()).slice(0,limit)}async search(q:string,limit=20){await ensureHydrated();return this.findAll({filter:{searchQuery:q},limit})}async findByStatus(status:OrderStatus,q?:OrderQuery){await ensureHydrated();return this.findAll({...q,filter:{...q?.filter,status}})}async bulkUpdateStatus(ids:string[],status:OrderStatus){await ensureHydrated();const s=new Set(ids);orders=orders.map(o=>s.has(o.id)?{...o,status,updatedAt:new Date()}:o);await persist()}}
export const orderRepository=new OrderRepository();
